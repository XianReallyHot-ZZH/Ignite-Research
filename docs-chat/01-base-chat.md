# 基础聊天

添加子模块 git submodule add https://github.com/apache/ignite vendors/ignite

检测标签 2.17.0，代码分支切换到该标签

## 探索

ignite 支持哪些 DDL 操作？怎么使用？API入口在哪？

以 IgniteCache.query(new SqlFieldsQuery(sql)) 为起点，研究 ignite 如何实现 CREATE TABLE 操作，梳理出完整的执行流程，并将流程进行大致的划分：客户端、服务端、数据存储层、网络通信层、分布式机制层，流程分析要有理有据，不能脱离源码。最终将研究结果写到 @docs-research 路径下。

深入分析 ignite 的数据存储层设计，拆解出有哪些核心概念、关键抽象、核心模块，关键设计，各自在解决什么问题，各个模块之间是什么关系，相互之间是怎么协作的。我是资深java工程师，但是之前没写过存储，所以要求详实、易懂，要有各种架构图、流程图等各种图表帮助我理解。结果写到 @docs-research 路径下。

将该项目的代码上传到 github 仓库，仓库地址为 https://github.com/XianReallyHot-ZZH/Ignite-Research.git


## 存储层探索

当前我在探索 ignite 存储层，docs-research/03-ignite-storage-layer.md 是前期对 ignite 存储层设计的探索结果，但是对我来说该文档过于深奥，理解曲线困难。
所以以下是我的提示词，用来指导 AI 进行进一步的拆解，来帮助我探索 ignite 存储层。提示词：

docs-research/03-ignite-storage-layer.md 是前期对 ignite 存储层设计的探索结果，但是该文档对于一个初学者来说，过于深奥，理解曲线困难。
现在要求你对 ignite 存储层进行深度的拆解，要求：
- 一开始不要融入过多的代码，要从原理出发，讲透原理了才能更好的理解代码实现
- 拆解的足够细致，每一个点的理解对读者来说要清晰、没有负担
- 每一个点要讲清楚，在解决什么问题，解决方案是什么，为什么这么设计
- 拆解的要足够完整，最终要对应到 ignite 存储层的完整实现
- 循序渐进，各个讲解的要点要像长出来的一样

请你对以上提示词进行评估，有没有更好的建议？有没有优化空间？



基于源码，你先检视一遍 docs-research/03-ignite-storage-layer.md 有没有明显的错误，内容是不是合理、完整、准确，有没有优化的空间，先把这个纲领性（天花板）的文档做到满分。


基于 docs-prompt/01-ignite-storage-layer-breakdown.md 文档，根据建议，先只产出"地图 + 第 1 阶"，给我试读,风格合适后,再按同一模具铺开剩下 5 阶


针对 02-row-link-freelist.md 文档，我提问：
1、一条行塞进页里时，槽位和行字节是如何变化的，给我一个‘动画’
2、一条行从页里删除时，槽位和行字节是如何变化的，给我一个‘动画’
3、数据页里是如何复用槽位的？复用的是indirect槽位？items 表里一个item 具体存的是什么？偏移的起点和终点？
4、pageId 是一个独立的对象吗？被存储管理在哪里？有专门的数据结构吗？我猜测不应该是放在数据页里的吧？我猜测应该是被管理在一个全局的对象里？
5、FreeList存的是具体的数据页对象吗？按桶分组管理数据页？
6、如果一行超过了可分配的页的空间，那么该行是如何存储的？槽位信息怎么记录？
不要猜测，基于源码进行分析，给我可靠的回答。


1、一个 item = 2 字节， direct item:[ 行的 dataOff(2B) ]：一个数据页 4KB，那么dataOff的最大值就是4096，并且不可能达到，一个 item = 2 字节，可以表达到 2的16次方的数字，完全没必要啊？只需要12位就足够表达，为什么这么设计？
2、一个 item = 2 字节， indirect item:[ itemId(高1B) | 指向的 direct item 索引(低1B) ]：itemId(高1B)应该是存的自己本身的id索引值？direct item 索引(低1B)存的是转移后的id索引值，然后那个direct item 索引对应的取值应该就是本身的id对应的dataOff对吧？然后一个数据页最多存256个item，所以这里至少两个字节，所以这样就解释了问题1？
3、槽位复用的问题：恒有 indirectCnt ≤ directCnt，这是为什么？然后你说‘当你 add 一行时,新 direct item 本该拿 itemId = directCnt。但如果正好有个 indirect item 的 itemId 等于这个值,就不新建槽了——把那个indirect item 直接转成 direct item(指向新行)’，但是这里面有个问题，外面的link里还存着这个indirect item的itemId，这样岂不是外面的link指向的数据永远找不到了？并且会找到错误的值？


1、一个 item 怎么判断是 direct 还是 indirect？
2、你说：indirect item 的作用,是在原主被删、末尾行被搬来借住期间,替那个搬动过的活行保留它的本命itemId;等插入时条件满足,就把活行接回本命槽、把死槽让给新行。为什么要设计成这样？死槽死在那不行吗？

item相关的东西我搞清楚了，然后我再问一些 数据页中行字节 的相关问题：
1、删除一个item后，数据页中行字节并没有改变位置，那么该数据页的剩余容量怎么计算？FreeList中拿到的页的剩余容量是不准的，因为是不连续的，这怎么解决？
2、然后一个数据页整理压缩是什么时候？具体是怎么整理压缩的？数据页的头、item表，行字节都怎么变化？数据页的变化会引起link的变化，进而会引起B+索引树的变化？


我再问一个问题：一个数据页里连续删多条row后，item表的表现是怎样的？


补一个边角:如果删的是已经被搬过的行(它的本命 id ≥ directCnt,要经 indirect 找),代码会先顺着 indirect 找到它现在所在的 direct 槽,再做上面的"搬末尾填洞"动作,同时把那个旧 indirect 收掉/改指，这也补充在上述item 表怎么变的案例里面






叶子页、叶子项；


B+树的叶子用图表具象化一下；
叶子按序链表相连，这个数据结构用图表具象一下；
叶子节点和叶子项是什么关系？
内部节点用图表具象化一下；
B+树用图表具象化一下；
B+树和SearchRow、DataRow的关系是什么？




## 建表+增加字段+增加索引在存储层的变化过程

我想研究 建表+表增加字段+表增加索引 这三个上层的操作在存储层分别是如何运转变化的，以下是我的提示词：
‘
基于前期阶段性研究成果：
- docs-research/03-ignite-storage-layer.md
- docs-research/storage-layer/
然后根据源码进行分析：建表+表增加字段+表增加索引 是如何影响存储层的？要求分析时：
- 流程详实，清晰，结合源码，不要猜测
- 图表优先，帮助理解变化过程
总结成md文档，放置在 docs-research/storage-layer-biz/ 路径下。
’
该提示词有没有优化的空间？



